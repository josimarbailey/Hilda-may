import React from 'react'

// TODO: Replace this placeholder with the full landing page code from the canvas.
// Your Calendly URL will still work after you paste your code.
// Make sure you keep the BrowserRouter wrapper in main.jsx.

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="p-6 max-w-lg text-center">
        <h1 className="text-3xl font-semibold">Hilda May Events</h1>
        <p className="mt-3 text-neutral-600">
          Starter project is live. Now paste your full landing page code into <code>src/App.jsx</code>.
        </p>
        <ol className="mt-4 text-left list-decimal list-inside text-sm text-neutral-700">
          <li>Open <code>src/App.jsx</code>.</li>
          <li>Replace this component with the code from the canvas.</li>
          <li>Commit & push — Netlify will redeploy automatically.</li>
        </ol>
      </div>
    </div>
  )
}
